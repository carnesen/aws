{
  "name": "@carnesen/aws",
  "description": "Source code for deploying to AWS",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "lint": "eslint src",
    "lint:fix": "npm run lint -- --fix",
    "test": "npm run lint && npm run test:integration",
    "test:integration": "NODE_ENV=test mocha --opts test/mocha.opts",
    "preversion": "npm test",
    "postversion": "git push && git push --tags"
  },
  "dependencies": {
    "aws-sdk": "2.48.0",
    "bluebird": "3.5.0",
    "expand-home-dir": "0.0.3",
    "fs-extra": "3.0.0",
    "keymirror": "0.1.1"
  },
  "devDependencies": {
    "chai": "3.5.0",
    "eslint": "3.19.0",
    "eslint-config-carnesen": "1.0.6",
    "mocha": "3.3.0"
  }
}
